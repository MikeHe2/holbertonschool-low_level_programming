#include <stdio.h>

/**
 * main - Prints single numbers from 0.
 *
 * Return: return 0
 */

int main(void)
{
	int num;

	for (num = 0; num < 10; num++)
	{
		printf("%d", num);
	}

	printf("\n");

	return (0);
}
